# 🚀 빠른 배포 가이드

## 📦 업로드할 파일 목록

```
✅ 업로드 필수 파일/폴더:

1. dist/index.html          → 서버의 /index.html
2. dist/assets/             → 서버의 /assets/
3. .htaccess                → 서버의 /.htaccess
4. api/                     → 서버의 /api/
5. data/                    → 서버의 /data/
6. images/                  → 서버의 /images/
```

## 🔄 배포 단계

### 1단계: 빌드
```bash
npm run build
```

### 2단계: FTP 업로드
**FileZilla나 WinSCP로 다음 파일들을 업로드:**

#### 📂 프론트엔드 (React 빌드 결과)
- `dist/index.html` → `/html/index.html`
- `dist/assets/` 폴더 전체 → `/html/assets/`

#### 🔧 백엔드 & 설정
- `.htaccess` → `/html/.htaccess`
- `api/` 폴더 전체 → `/html/api/`

#### 📊 데이터 & 이미지
- `data/` 폴더 전체 → `/html/data/`
- `images/` 폴더 전체 → `/html/images/`

## 🌐 배포 후 서버 구조

```
/html/                          (서버 루트)
├── index.html                  ← dist/index.html
├── .htaccess                   ← .htaccess
├── assets/                     ← dist/assets/
│   ├── index-abc123.js
│   └── index-abc123.css
├── api/                        ← api/
│   ├── check-auth.php
│   ├── config.php
│   ├── login.php
│   ├── logout.php
│   ├── news.php
│   ├── people.php
│   ├── upload-image.php
│   └── work.php
├── data/                       ← data/
│   ├── work.json
│   ├── news.json
│   └── people.json
└── images/                     ← images/
    ├── fishermen.png
    ├── witness.png
    └── uploads/
```

## ✅ 배포 완료 후 테스트

### 1. 메인 페이지 접속
```
https://yourdomain.com/
```

### 2. 관리자 로그인 테스트
```
https://yourdomain.com/admin
```
- 아이디/비밀번호 입력 후 로그인
- 대시보드로 정상 이동하는지 확인

### 3. 각 페이지 테스트
- `/work/` - 사역 페이지
- `/news/` - 소식 페이지
- `/people/` - 청년부원 페이지

### 4. Witness/Fishermen 토글 테스트
- 토글 버튼으로 그룹 전환 확인
- URL에 `?group=witness` 파라미터 확인

## 🔄 업데이트 시

### 프론트엔드만 수정했을 때:
```bash
npm run build
```
→ `dist/` 폴더 내용만 다시 업로드

### 데이터만 수정했을 때:
→ `data/` 폴더의 JSON 파일만 업로드

### PHP 수정했을 때:
→ `api/` 폴더만 업로드

## ⚠️ 주의사항

1. **파일 권한:**
   - `data/*.json` → 666 (읽기/쓰기)
   - `images/uploads/` → 777 (업로드 가능)

2. **PHP 버전:**
   - PHP 7.4 이상 필요
   - `mod_rewrite` 활성화 필요

3. **첫 배포 시:**
   - `.htaccess` 파일 반드시 업로드
   - 숨김 파일 표시 옵션 활성화

## 🐛 문제 해결

### "404 Not Found" 에러
→ `.htaccess` 파일이 업로드되었는지 확인

### 관리자 로그인 실패
→ `api/login.php` 권한 확인 (644)

### 데이터가 안 보임
→ `data/*.json` 파일 경로 확인

---

✅ 배포 완료!
